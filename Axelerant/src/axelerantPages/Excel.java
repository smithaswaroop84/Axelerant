package axelerantPages;

import java.io.File;
import java.io.FileInputStream;

import org.apache.poi.hssf.usermodel.HSSFWorkbook;

import org.apache.poi.ss.usermodel.Row;

import org.apache.poi.ss.usermodel.Sheet;

import org.apache.poi.ss.usermodel.Workbook;

import org.apache.poi.xssf.usermodel.XSSFWorkbook;

//import org.apache.poi.xssf.usermodel.XSSFWorkbook;

public class Excel {
	
/*	File f = new File("Excel path");
	FileInputStream fis = new FileInputStream(f);
	XSSFWorkbookFactory wb = new XSSFWorkbookFactory(fis);
	Sheet wsheet = guru99Workbook.getSheet(sheetName);

    //Find number of rows in excel file

    int rowCount = wsheet.getLastRowNum()-wsheet.getFirstRowNum();

       for (int i = 0; i < rowCount+1; i++) {

        Row row = wsheet.getRow(i);

        //Create a loop to print cell values in a row

        for (int j = 0; j < row.getLastCellNum(); j++) {

              System.out.print(row.getCell(j).getStringCellValue()+"|| ");

        }
*/
}
